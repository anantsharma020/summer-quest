// ============================================================================
// Cloud accounts config (optional).
//
// Fill these from your Supabase project → Project Settings → API to enable
// email/password accounts + cloud sync. BOTH values are safe to be public:
// the anon key only grants access through row-level security (each user can
// only read/write their own row). Passwords are handled by Supabase Auth and
// are never stored in this app.
//
// Leave both blank to keep the app fully local-only (guest mode).
// ============================================================================
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
